import { AREA_CONFIG } from './pci-model.js';

// Reglas de estructura de la rama de prueba.
// Se aplican antes de iniciar app.js para que la migración use esta configuración.
AREA_CONFIG.Tecnologías.count = 8;
AREA_CONFIG['Otros formatos pedagógicos'].sourceArea = 'Otros formatos pedagógicos';
